{
  "name": "cascade-cards-monorepo",
  "version": "0.3.0",
  "private": true,
  "description": "CK3-style wiki hovercards for SaaS apps",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "turbo": "^1.12.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.15.0",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "dependencies": {
    "tailwindcss-animate": "^1.0.7"
  }
}
